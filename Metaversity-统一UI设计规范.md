# Metaversity 统一 UI 设计规范

版本：v0.11 · 展示排版与业务样例 · 2026-09-15

本文件集中定义各模块共用的视觉与交互规则，供后续设计、前端开发和界面验收参照。业务功能仍以各模块文档为准。本文件不规定首页有哪些区块、容器放在哪一行或具体页面的内容顺序。

## 1 已确认范围与使用方式

用户已确认：

- APP 为主要设计对象，需要全项目共用的 UI 规范。
- 底部一级导航目前只有「首页」「个人」。这不代表已经确认个人页的具体功能。
- 容器是功能入口的一种形式。首页可以同时包含轮播图、图文内容、功能入口和可上下滚动的页面内容。
- 视觉质量需要参考成熟 APP；不能将首页简化为一组外观相同的空入口。
- 首页顶部移除品牌与学校选择栏，改用紧凑全局搜索；学校切换放在个人页，所有业务页不展示公共演示环境横幅。
- 相关列表的筛选默认收起，按需在覆盖内容的浮层中展开；起止日期或时间共用一个范围入口，按开始、结束两步选择。此规则适用于后续新增和修改的同类页面。

字体、文字层级、表单必填标识、列表筛选浮层、时间范围交互、紧凑导航、整框入口与下拉选项样式已确认统一，应用与 Metaversity-Vision 展示页按本版本执行。其余配色、间距、圆角沿用现有视觉基线，仍可继续讨论；不将局部确认扩展为全部视觉定稿。首页布局仍由实际内容决定。

## 2 全局视觉原则

1. 通过文字层级、内容密度、图片构图和留白建立重点。装饰效果服务于内容表达。
2. 全部业务共用颜色角色、字体层级、间距档位和组件状态。不同内容可以使用不同的组件变体。
3. 每个局部区域有清晰的阅读顺序；相关内容靠近，同级内容对齐。容器尺寸根据内容需要选用，不规定全站等大。
4. 学校照片、建筑插画、视频封面承担校园特色表达；业务信息使用清晰、可读取的文字。
5. 特殊视觉效果必须同时考虑文字可读性、滚动表现和加载成本，并在组件或素材规范中统一定义。

### 2.1 页面任务与内容组织

- 开始设计前，用一句话明确谁在使用、要完成什么、最先需要看到什么；结合已有页面、内容和素材选择组件。改版先检查现有阅读顺序、主要操作、移动端布局和状态反馈，再针对具体问题调整。
- 课表、预约、办事和订单等高频业务优先展示关键值、当前状态和下一步操作；同类记录使用稳定的对齐与顺序，便于快速扫描和比较。
- 学校介绍、校史、文化和图集等内容展示通过主图、次级图文与简短列表形成主次。先确定内容的重要程度，再分配版面，不把所有内容套进相同大小的卡片，也不为追求变化打乱同类信息的秩序。
- 首屏优先保留页面主题、必要查询入口和核心内容；标题下的说明直接解释用途，不用大面积留白、重复口号或装饰横幅把任务入口推到屏外。文字放大后允许自然延长页面。
- 一个区域围绕一个明确主题组织。说明紧随标题，时间、地点与状态靠近对应对象；同一区域突出一个主要操作，相同动作使用一致名称。
- 分区通过内容类型、图片比例、宽度和留白形成节奏。避免连续重复大图配空泛介绍，也不机械要求每个分区更换布局。网格按实际内容数量组织，不增加占位卡片来凑齐行列。
- 每个多列组合明确窄屏时的阅读顺序、列数与换行方式。内容图文可改为单列，快捷入口继续按 5.1 保持适合触控的分组；关键值与主要操作不因布局收紧而隐藏。

## 3 颜色系统

### 3.1 颜色角色与候选值

本轮采用「中性浅灰背景、白色表面、校园蓝强调」。主色 #016AED 直接参考[上海交大交我办官方界面规范](https://developer.sjtu.edu.cn/form/standard/pageStyle.html)；按压色、浅底色、中性色及圆角由本项目配套定义，不冒称为四校官方色板。对标来源与取舍见[研究记录](https://github.com/wanwuguodu/Metaversity-Vision/blob/main/docs/高校校园移动服务视觉对标.md)。后续若改主色，应成组调整主色、浅底色、按压色及其文字色，不在各页面分别换色。

| 统一名称 | 候选值 | 用途 |
| --- | --- | --- |
| color.background | #F5F6F8 | 页面基础背景 |
| color.surface | #FFFFFF | 普通内容卡片、表单和面板 |
| color.text.primary | #1F2329 | 标题、正文、关键数据 |
| color.text.secondary | #606875 | 补充说明、时间和次要信息 |
| color.border.subtle | #E5E8EE | 装饰性分隔与轻边界，不独自承担交互控件识别 |
| color.border.control | #7C8798 | 需要明确识别的输入框边界 |
| color.brand.primary | #016AED | 主要操作、选中状态、品牌强调 |
| color.brand.pressed | #0058C7 | 主色实心按钮按压态 |
| color.brand.soft | #F2F7FF | 主色浅背景，配主色文字 |
| color.onBrand | #FFFFFF | 主色实心背景上的文字和图标 |
| color.disabled.background | #E9ECF0 | 禁用控件背景 |
| color.disabled.foreground | #6D7580 | 禁用控件内容 |
| color.scrim | #000000，32% 不透明度 | 弹层背景遮罩 |

### 3.2 业务状态色

| 语义 | 前景色 | 浅背景色 | 使用规则 |
| --- | --- | --- | --- |
| 成功 success | #257346 | #EAF5EE | 配合「已完成」等文字或成功图标 |
| 警告 warning | #8A5B12 | #FFF4DF | 配合需要注意的原因及后续操作 |
| 错误 error | #B42318 | #FFF0EE | 校验错误、失败及明确的危险操作 |
| 信息 info | #2D5E9D | #EDF3FC | 普通提示及说明 |

蓝色用于操作和选中，绿色用于成功，两者职责分开。不能用颜色单独表达成功。状态同时使用准确文案，必要时配图标。课程分类、地图类型等需要额外颜色时，统一登记分类色及其文字搭配，不占用警告、错误等语义。

### 3.3 用色约束

- 页面大面积使用基础背景、内容表面和真实图片；主色用于少量重点。不同业务不各自换一套背景和按钮色。
- 搜索和高频操作保持明确视觉层次；常用、分类、推荐等区块以标题和间距区分。它们是组件组织方式，不规定具体首页区块或顺序。
- 彩色插画、照片和数据图可以拥有多种颜色，交互控件仍使用共用角色。
- 普通文字与实际背景对比度至少 4.5:1；本项目标题也优先采用同一门槛。文字叠加图片时使用稳定的底色或遮罩，在实际图片上检查。
- 控件识别边界、关键图标和选中标记需要清晰可辨；纯装饰分隔线允许更轻。禁用态不得用来呈现用户仍必须读懂的重要说明。
- 深色模式是否交付另定；如交付，必须提供独立的成对色值和状态检查，不能直接反转图片与页面颜色。
- 同一页面保持连续的明暗关系和中性色温，不以突然反色制造分区。渐变、玻璃、纹理和光晕仅在具体素材或层级表达需要时使用，不叠加到所有卡片；透明表面同时提供不依赖模糊效果的可读底色。

候选色板已计算的部分组合：白字／主色 4.90:1，主色字／主色浅底 4.56:1，主文字／页面背景 14.59:1，次文字／页面背景 5.20:1；四种状态前景／对应浅底均超过 5:1。此结果仅验证色值搭配，不代表界面审美或图片叠字已通过验收。

## 4 字体与文字

统一使用 **阿里巴巴普惠体 3.0（Alibaba PuHuiTi）**，中文、英文和数字共用该字体家族。正文采用 Regular（400），标题、按钮、字段名称与强调文字采用 Medium（500）。系统字体只用于字体尚未加载或缺字时的回退；代码示例可使用系统等宽字体。

字体来自[阿里巴巴字体官网](https://www.alibabafonts.com/)，商业使用遵守官网链接的[普惠体 3.0 法律声明](https://www.yuque.com/yiguang-wkqc2/hgpff0/nus9wiinq4aeiegy)。应用 `frontend/public/fonts/` 与设计仓库 `assets/fonts/` 保存相同的官方原版 WOFF2、完整中英文 `LICENSE.txt`、来源说明与 `typography.css`，不自行改造或裁切字库。

Web 以默认根字号 16px 下的 CSS px 描述，代码使用 rem 和可随字号缩放的行高。手机（视口不大于 600px）和电脑共用文字角色，采用各自的尺寸档位；根字号保持 100%，不缩放整页。后续原生端按同一语义映射系统逻辑单位。

| 文字角色 | 电脑尺寸／行高 | 手机尺寸／行高 | 字重 | 用途 |
| --- | --- | --- | --- | --- |
| heading | 24／32 | 20／28 | 500 Medium | 页面主标题 |
| section | 18／26 | 16／24 | 500 Medium | 分区、卡片标题 |
| body | 16／24 | 14／20 | 400 Regular；强调时 500 | 正文、按钮、字段名称与功能入口 |
| input | 16／24 | 16／24 | 400 Regular | 实际输入框、下拉选择与多行输入中的文字 |
| secondary | 14／22 | 13／20 | 400 Regular | 辅助说明、错误提示、时间与普通导航 |
| caption | 12／18 | 12／18 | 400 或 500 | 仅限地图、课表等紧凑标注，不用于正文或错误提示 |

- 字体与文字层级统一引用 `typography.css` 的 `--font-ui`、`--weight-*`、`--text-*` 和 `--leading-*`；不在各模块自行定义字体栈或散落字号。新增档位先同步本规范及展示页。
- 原版字体文件由各自站点同源提供，使用 `font-display: swap`，常规字重预加载，Medium 按需加载。两个完整字库合计约 10.7 MB（十进制）；首访下载期间先显示回退字体，后续使用浏览器缓存。
- 禁止浏览器合成粗体与斜体；不通过负字距压缩中文，使用 Medium 与字号、留白建立层级。输入内容保持 Regular，字段名称与按钮使用 Medium。
- 功能名称优先自然换行，不能把长中文缩小到难以辨认。摘要超出规定行数可以省略，完整内容在详情中读取。
- 金额、时间、成绩、错误原因等关键内容必须完整展示。
- 日期、时间、数字单位由同一格式规则输出；时间使用 24 小时制，单位与数字保持一致的间距。
- 支持浏览器文字放大；放大后可增加组件高度或改为纵向排列，不能截掉按钮文字与主要操作。字体尺寸变化不改变业务校验、必填范围或日期选择规则。

### 4.1 界面文案

- 标题直接说明内容，按钮使用明确的动作与对象，辅助文字只补充用户作决定需要的信息。避免空泛口号、牵强比喻和重复解释页面已有内容。
- 分类标签、编号、状态点和图片角标必须表达真实分类、顺序或状态；不为每个标题添加英文小字、装饰序号或无意义标签。真实步骤、图集位置和规范章节可以保留编号。
- 长说明按主题分段，摘要可以引导进入全文；办理条件、金额、时间、错误原因和操作后果不能为了短句或固定行数而删减。长列表按任务选择搜索、筛选、分组或分页，不为了视觉变化强制改成轮播。
- 数据、人物、评价和学校事实须有实际依据；演示内容明确说明为示例，不编造精确指标、用户评价或合作背书来充实版面。
- 交付前通读可见文案、图片替代文本和各状态提示，检查指代、单位、动作名称与实际结果，删除重复、含混或不自然的表达。

## 5 尺寸、留白与表面

布局尺寸使用逻辑单位：iOS 使用 pt、Android 布局使用 dp、Web 使用 CSS px；最终按目标设备核对。字体单独遵循上一节规则。

| 项目 | 建议档位 | 用法 |
| --- | --- | --- |
| 间距 | 4、8、12、16、24、32 | 图文内间距到大区块间距依次选用 |
| 常规页面边距 | 16 或 24 | 同类页面保持一致；全幅图片等组件可延伸至边缘 |
| 圆角 | 6、8、12、20 | 小标签、控件、卡片、大面板；完整胶囊形作为单独变体 |
| 图标框 | 16、20、24 | 内联提示、常规图标、导航与主要操作图标 |
| 常规操作高度 | 电脑至少 48；手机至少 44 | 内容放大或换行时允许增高，尺寸收紧不改变触控下限 |
| 紧凑筛选操作高度 | Web 至少 44 | 搜索、筛选入口及浮层内普通控件；文字按设备角色档位，实际输入文字保留 16/24，长内容可增高 |
| 点击区域 | iOS 至少 44×44；Android 至少 48×48 | 图标外可增加透明点击区，相邻区域不能重叠 |

常规内容卡片采用 12 圆角与白色表面，标题、数字及细色条可突出重点；不默认使用整张深色填充。20 圆角留给大面板。卡片通常使用表面色和留白建立边界；轻描边、阴影按组件变体选用。阴影只用于需要表达浮起关系的控件、面板或导航，统一定义普通表面、浮起表面、遮罩弹层三层关系。不能由各页面自由叠加不同阴影。

卡片用于形成独立内容或操作边界；同一组简短记录可共用一个表面，以对齐、间距和必要的分隔线组织，避免层层套框。图片、标题与摘要按内容确定高度，不为对齐强制拉伸图片或留下大块空白。相同用途沿用同一圆角和表面样式，不逐张添加不同材质。

页面需考虑系统状态栏、底部手势区和键盘。内容区与安全区分开处理，避免重复留白或操作被遮挡。不得通过锁死整个页面高度换取某一张截图整齐。

### 5.1 手机内容密度

- 手机字体、图标、内边距和容器最小高度成组调整，不直接缩放整页；电脑端保留现有比例。
- 首页功能图标一般为 20px，课表主图标 24px；常用入口卡片以 76px 最小高度、上下 8px 留白和 6px 图文间距起步，长名称自然增高，不固定为方形。
- 学习卡片上下分区使用较紧凑留白；普通服务行至少 52px，主要按钮和输入点击区至少 44px。共用面板手机内边距 16px，短列表项内边距 12px。
- 手机保持既有三列快捷入口和两列服务分组，文字完整换行。容器由内容撑开，文字放大后不能截断功能名、输入值或操作。

## 6 图片、插画与图标

- 通用操作图标使用同一图标家族，统一线宽和尺寸。品牌插画、立体分类图标可以单独设计，但同组必须保持相同材质、视角、光线和细节程度。
- 内容图片按用途规定比例：横幅优先 16:9 或 2:1，普通图文封面 4:3，方图 1:1；同一组件变体保持一致。具体选择由内容用途决定，不固定首页使用哪种比例。
- 裁切时保留人物、建筑和标识的重点区域；不能拉伸图片。加载前保留比例空间，图片失败后保留文字与可用操作。
- 图片上的标题应保持可读并适配长中文；承载业务信息的文字优先独立渲染。已确认的 APP 学校身份加载合图是品牌素材特例，按对应文档保留完整构图及可访问描述。
- 视频默认由用户主动播放；如后续确认使用背景视频，需提供静态封面及减少动态效果时的替代呈现。
- 优先使用与内容直接相关的校园照片、原始文献图像和实际产品画面。建筑、人物和活动记录不以生成图片替代事实；插画可以表达抽象主题，但不能让用户误认作真实场景。
- 素材的构图、比例与文字位置一并设计；同组插画和摄影处理保持一致。图像本身已有足够信息时，不再叠加无关标签、装饰署名或大量文字。
- 展示产品操作时使用实际组件或真实截图，示例清楚表达可操作范围。信息查询和填写页面无需为了装饰增加图片；缺少素材时保留明确的缺图状态及内容入口。
- 图片按展示尺寸提供合适资源，首屏关键图片优先加载，非首屏素材按需加载；提前保留图片、视频与嵌入内容的尺寸，避免加载时推移文字和操作。

## 7 共用组件规则

| 组件 | 全局需要统一的内容 |
| --- | --- |
| 按钮 | 主要、次要、文字、危险四种用途；相同用途采用同样的高度、文字和反馈。同一决策区域突出一个主要操作 |
| 功能入口／容器 | 单一操作入口整框可点击，图标、标题、摘要和留白共用一个语义入口；图标不另设小按钮。按 7.3 统一实现 |
| 内容卡片／列表项 | 标题、正文、时间、状态和操作有明确层次；卡片整体点击与内部按钮不能产生冲突 |
| 搜索、筛选与标签 | 搜索常驻、条件默认收起，使用不推挤列表的浮层；条件数量和选中状态明确，按 7.1 统一实现 |
| 下拉选择 | 完整输入框触发圆角选项浮层，统一字形、选中与键盘状态；按 7.4 统一实现 |
| 日期与时间范围 | 一个入口并排显示起止值，先开始、后结束，确认后成对生效；按 7.2 复用中文滚轮 |
| 输入框与表单 | 字段名称持续可见；必填项在名称旁显示红色 `*`，选填项不加标记，不使用「（必填）」「（选填）」「（可选）」后缀或整表必填说明；占位文字不能替代字段名称 |
| 弹窗与底部面板 | 简短确认使用弹窗，局部详情及选择使用面板；复杂任务进入页面。关闭、返回和未保存内容处理遵循相同规则 |
| 轮播与图集 | 显示当前位置与前后切换按钮；图片顺序稳定，大图区域统一缩放和移动，不使用拖动翻页 |
| 提示与结果 | 短暂提示承载轻量反馈，持续错误或待处理事项保留在页面；提交结果需要明确下一步 |

每个组件按适用情况提供正常、按压、选中、禁用、加载、错误状态；不适用的状态无需硬加。组件的变体、状态和文案约束写在组件说明中，由所有使用页面共用。

必填星号统一使用错误色 `#B42318`，Web 实现复用 `RequiredMark`；控件同步设置 `required` 或对应无障碍语义。条件必填的标记随条件变化，例如拒绝访客申请时才标记拒绝原因。前端提供填写反馈，后端独立校验缺失、空值、全空白、格式、长度与条件规则并返回明确错误，不能只依赖星号或浏览器校验。

### 7.1 列表搜索与筛选浮层

- 所有新增和修改的相关目录、搜索结果及记录列表必须沿用本设计。应用统一复用 `components/common/filter-bar.tsx` 的 `FilterBar`，由 `components/common.tsx` 导出；不为业务模块另建默认展开的长条筛选面板。
- 页面默认只显示必要的搜索和「筛选」入口，为核心列表保留首屏空间。无搜索的列表只显示筛选入口，无筛选项时不显示空入口。一级导航、页面切换及购物车、创建等主要操作保留在页面上；业务填写与提交表单不放入筛选浮层。
- 电脑端在筛选按钮下方弹出锚定浮层，遇视口边缘自动避让；当前 Web 宽度上限 560px、最大高度 80dvh。600px 及以下使用底部浮层，最大高度 85dvh，保留底部安全区。两者覆盖内容，展开和收起不得改变结果列表的文档位置。
- 浮层包含标题、关闭入口、条件区和「查看结果」按钮。条件较多时只滚动条件区，标题与底部按钮保持可达；点击遮罩或按 Escape 可关闭，关闭后焦点返回筛选入口。日期范围小窗显示在筛选浮层上方，关闭后返回范围入口。
- 短选项按内容宽度并排，列间距 16px、行间距 12px；长标签、范围入口和多选组允许占整行。窄屏或文字放大时自然换行，不能通过缩小字号或截断关键值硬塞进一行。
- 搜索与普通筛选控件使用至少 44px 的 Web 点击高度和 8px × 12px 内边距，按钮及字段名称沿用当前设备正文档位，实际输入文字为 16/24。范围入口可按两层文字增高；系统原生端仍遵循各平台命中区域要求。
- 筛选入口显示当前有效条件数量，一组起止范围计为一项。普通条件沿用原业务的即时查询与重置行为；「查看结果」关闭浮层，关闭不会撤销已生效的普通条件。搜索关键词与条件的清除行为不得悄悄改变所属业务规则。

### 7.2 日期、时刻与起止范围

- 单日期、时刻和日期时间统一使用 `components/wheel-picker/` 的 `WheelPicker`；整个输入框可打开中文滚轮，不使用原生日期/时间输入或手填时间戳。受限时刻与时长沿用真实可选项。
- 同一筛选范围的开始与结束统一使用 `components/wheel-picker/range.tsx` 的 `WheelRangePicker`。一个可点击框内并排显示起止标签和值，中间用方向标记连接；空值显示「不限」，必填空值显示「请选择」。
- 打开后先选择开始，确认开始后自动进入结束步骤，明确显示当前步骤及已选开始值；允许返回修改开始。最终确认一次性更新两个值，取消或关闭保留进入前的原范围，不产生半个范围的查询。
- 结束下限随开始同步，保留业务原有的最大范围、日期精度、时区、空值和必填规则。支持单侧空值的筛选可分别选择「不设开始／结束」；要求完整租期的业务必须成对选择，归还时间须晚于领取时间。必填范围不能清空。
- 范围控件复用既有 `wheel.tsx` 滚轮与 `values.ts` 日期计算；支持触摸、鼠标和键盘。展示 HTML 只演示交互，不替代实际业务可用时段和前后端校验。

### 7.3 整框入口

- 只有一个进入、查看或选择操作的卡片，整个可见容器都是点击区；图标与箭头仅作视觉提示，不建立第二个按钮。
- 已使用整行按钮的入口继续复用；内容较丰富的入口通过公共 `CardEntry` 与 `entry-card` 将单一语义按钮的范围覆盖整个容器，保留标题、图片、摘要与来源阅读结构。Tab 只停靠一次，焦点环覆盖完整容器，Enter 或 Space 可触发。
- 入口名称包含所指内容，不能只留下没有名称的小图标。按压、悬停、焦点反馈覆盖完整形状；手机点击区至少 44px，不因视觉图标较小而缩小。
- 纯展示面板、表单、多操作面板不赋予一个隐含主操作；图片失败的重试和实际业务操作保留独立语义与点击边界，不嵌套按钮或误触提交。

### 7.4 圆角下拉选择

- 应用统一复用 `components/common/select.tsx` 的 `Select`、`SelectOption`，基于现有 Base UI Select；不再使用操作系统原生下拉菜单。单选、多选均使用同一外观与交互。
- 触发框完整可点，圆角 8px，文字与表单统一 16/24 Regular，箭头置于框内。选项浮层与触发框间距 6px，圆角 12px，白色表面、轻边界与轻阴影，宽度至少与触发框一致并受视口限制。
- 浮层内边距 6px，选项圆角 8px、内边距 10px 12px、最小高度 44px；文字使用正文档位（电脑 16/24，手机 14/20）。选中项使用浅蓝底、蓝色 Medium 文字与右侧勾选，悬停或键盘定位另有清晰浅底。
- 长列表内部滚动，浮层自动避让视口边缘；在筛选浮层之上打开，选择不推挤页面内容。支持方向键、Home/End、字符定位、Enter/Space、Esc 和 Tab；关闭后遵循焦点返回规则。
- 保留原有选项、值转换、动态联动、空值、禁用和必填范围。多选逐项勾选并显示已选值，不依赖 Ctrl 点击；必填校验继续执行，后端校验独立生效。
- 静态 HTML 以原生 JavaScript 演示相同圆角和键盘交互；保留原表单值与筛选行为，示例不代替应用组件。

## 8 导航、滚动与动效

- 一级页顶部只保留全局搜索框，搜索范围为功能入口与功能内的大分类，不检索具体人员、活动、商品或其他业务内容。输入框与搜索操作保持 44px 最小高度；搜索输入文字使用 16/24，按钮使用当前设备正文档位。支持提交、清空、空结果反馈及返回，分类结果显示所属功能，点击进入对应区域。
- 顶部、底部导航按 44px 控件加上下各 4px 留白布局，默认约 53px（含 1px 分隔线，不含系统安全区）。文字放大时允许增高；不能压缩文字或取消安全区来追求固定高度。二级页保留紧凑的返回与标题。
- 顶部不重复显示品牌和学校选择；学校切换归个人页，切换继续清理原学校上下文。业务页面移除公共的演示环境提示横幅。

- APP 底部一级导航固定为「首页」「个人」，使用图标加文字，两个入口同等分配空间。白色底栏配中性灰未选中项，选中项使用蓝色、加粗图标与短标记，不为每个导航项铺大块浅色底。选中状态不只靠颜色。
- 一级导航用于切换主区域，不承担提交、添加等局部操作。详情页是否显示底栏由统一页面类型规则决定，建议普通详情页收起底栏并显示返回入口。
- 一级区域切换及详情返回保留合理的浏览位置、筛选条件和已加载内容；主动刷新或业务失效另作说明。
- 内容页面保持正常纵向滚动；地图和大图画布内的手势按 8.1 执行。图集通过前后按钮切换，不把图片移动解释为翻页；与系统返回手势的冲突需实际核对。
- 轮播默认手动切换。需要自动轮播时单独确认间隔、暂停方式和必要文案可读时间；关键内容不能只能等待轮播才出现。
- 反馈动效建议 120–180 毫秒，页面及面板过渡建议 220–320 毫秒；同类交互统一方向和节奏，优先沿用系统转场。
- 减少动态效果设置开启时，简化大幅位移、视差和自动动画。动效不延迟实际操作结果，不为展示动画强制等待。

### 8.1 图片与地图的缩放、移动

- 所有现有及后续地图、校历、原图和大型图片查看区域统一：手机双指缩放、单指直接拖动；电脑滚轮缩放、按住鼠标右键拖动。鼠标左键保留地点和业务入口的点击语义，不承担画布拖动。
- 缩放以鼠标所在位置或双指中点为中心；双指平移与缩放一并计算。双指变单指、指针取消、失去捕获或窗口失焦后不跳变、不残留拖动状态；手势后的点击不能误开地点或翻页。
- 只在画布范围处理滚轮、触摸和右键菜单，页面其他区域保留浏览器滚动和右键行为。缩放范围由原业务决定；图片缩回适配尺寸时居中，放大后移动不能把整张图拖出视野。视口变化后重新约束边界。
- 地图与大图均不显示放大、缩小、重置／适配按钮、缩放比例或缩放工具条，也不预留这些工具的位置。缩放和移动直接在画布内完成；后续同类组件不得再添加这类按钮。图片画布使用 12px 圆角，高度最多 64dvh；画布外保持正常页面滚动。地图沿用其业务画布高度与范围。
- 按主要输入设备显示一行辅助提示：「双指缩放，单指拖动」或「滚轮缩放，按住右键拖动」。键盘聚焦画布后，加减键缩放、方向键移动、Home 重置；地点入口仍能独立聚焦；键盘功能不增加可见缩放按钮。
- 应用地图复用 `lib/pan-zoom.ts` 的 `bindPanZoom`；图片查看复用 `components/common/zoomable-image.tsx` 的 `ZoomableImage`，保留 `SafeImage` 的授权、加载和失败重试。普通卡片缩略图不截获手势，继续服从整框入口规则。
- 两份 HTML 的图集和原图查看演示同一规则，内联手势函数与应用公共函数同步。日期与时间滚轮、普通列表和浏览器整页文字缩放不属于图片画布，不改用这套手势。

### 8.2 动效用途与实现

- 每个动效必须说明它表达的操作反馈、状态变化或空间关系，例如收藏后的选中反馈、面板进入与退出；静态信息保持静止，不为卡片添加持续漂浮、闪烁或鼠标追随效果。
- 高频任务不依赖入场动画才显示内容，不劫持普通纵向滚动，不设置必须等动画结束才能点击的入口。悬停反馈必须有触控与键盘对应状态。
- 优先用 CSS 的 transform、opacity 完成位移与显隐，避免连续改变 top、left、width、height 造成反复布局；只声明需要过渡的属性，不使用 transition: all。will-change 仅用于确实会持续变化的元素。
- 拖动、缩放等连续输入使用现有手势实现，不通过每帧更新整个页面状态驱动。新增监听器、计时器和动画在组件退出时清理，不为简单反馈引入新的动画库。
- 所有非必要动效均响应系统减少动态效果设置：取消位移、缩放、视差和循环动画，保留即时状态、文字与可用操作；页面内的减少动态效果开关也执行相同规则。

## 9 加载、空内容、失败与完成

| 状态 | 统一表达 |
| --- | --- |
| 初次加载 | 结构已知时显示对应占位，短操作使用加载反馈；保持页面结构稳定 |
| 后续刷新 | 有可用内容时保留内容，并在对应区域说明刷新状态 |
| 确认无数据 | 明确说明没有什么内容；有合理后续操作时提供入口 |
| 搜索无结果 | 保留关键词及筛选条件，提供调整或清除条件的方式 |
| 请求失败／离线 | 说明未能获取或保存，提供重试；不得显示为无数据或操作成功 |
| 不可访问／不可操作 | 说明可公开表达的原因及可用下一步；具体权限规则仍由后续业务方案确定 |
| 提交中 | 阻止重复提交并保留输入；超时后不能未经查询就声称提交失败或成功 |
| 已提交／已办结 | 依据实际系统结果分别表达；申请提交成功不等于审核通过或办理完成 |

文案使用清晰的动词和对象，例如「查看课表」「重新加载」「保存修改」。错误信息说明问题及用户可做的下一步，不直接展示堆栈、接口名或内部错误码。

## 10 开发引用与交付验收

1. 将颜色、字号、间距、圆角和动效统一维护为主题参数，即设计 Token。业务页面引用语义名称，具体技术文件形式在框架确定后选择。
2. 共用组件集中维护。新页面先选已有组件与变体；确有新需求时补充组件说明，再供其他模块复用。
3. 每个交付页面标明采用的规范版本、组件变体、素材和适用状态；出现特殊规则时记录具体原因和适用范围。
4. 验证真实或有代表性的内容，包括长中文名称、缺图、空数据和错误状态；检查窄屏、常规屏、大字体、键盘和系统安全区。
5. 页面合格需同时满足：参数与组件一致、文字可读、主要内容无遮挡、点击与返回正确、状态表达真实。只对比正常状态截图不足以验收。
6. 若共用参数变化，统一更新受影响组件并核对代表页面；业务功能变更仍回到对应模块文档。本规范不自行新增后台、权限或第三个一级导航。
7. 相关列表必须核对默认收起、展开不移动结果、手机浮层独立滚动、关闭后焦点恢复、条件数量、搜索与清除，以及起止范围的顺序、上下限和取消回退；不能只验收展开后的静态截图。两仓库规范及 HTML 同步更新，应用 HTML 仅允许字体资源引用路径不同。

### 10.1 设计质量检查

按页面实际内容和交互检查适用项，发现问题后在对应组件或文案中修正：

| 检查对象 | 通过条件 |
| --- | --- |
| 任务与主次 | 能直接看出页面用途、重点内容与下一步；首屏没有挤占任务空间的重复说明或装饰 |
| 内容与布局 | 同类信息便于比较，图文主次符合内容重要程度；没有凑数空格、无意义嵌套或强制等高造成的大块空白 |
| 文字与视觉 | 使用统一字体和颜色角色；长中文、图片叠字、按钮、输入、焦点与错误提示在实际背景上清楚可读 |
| 素材与文案 | 图片完整加载或有可用替代；文本自然准确，数据与事实有依据，示例不会被误认为正式结果 |
| 手机与放大 | 多列顺序合理，无页面横向溢出；关键值、触控区域、安全区与主要操作完整，不能只检查默认字号 |
| 状态与交互 | 检查适用的加载、空结果、错误、禁用、提交和完成状态；键盘可达、焦点可见，返回与取消行为正确 |
| 动效与稳定性 | 状态即时生效，减少动态效果后仍可完成操作；素材加载与动画不造成明显跳动，滚动和连续输入无明显卡顿 |

修改涉及图片、动画或大面积布局时，在代表性视口查看实际渲染，必要时记录浏览器性能检查结果；未测量的加载与交互指标不写成已达标。现有页面改版同时复核入口、字段与浏览状态，视觉调整不改变业务含义。

### 10.2 规范展示与业务样例

- 展示页直接呈现章节、参数与操作样例，说明使用普通文档排版，不为每段说明添加卡片、胶囊或重复英文标题。规范文档画布采用白色；需要展示业务背景与组件边界时，在样例区域内呈现对应表面，不改变业务颜色角色。
- 字号、尺寸等参数按含义排列；简短说明置于分区标题下，不独占一个网格单元。并排样例顶部对齐，窄屏按阅读顺序排列，不能留下由空网格造成的大块间隔。
- 业务样例只呈现用户需要的信息、状态与操作，组件尺寸、布局理由和演示操作说明放在样例外。使用一致的校园任务上下文检验中文长短、步骤、时间和状态；示例身份清楚表达，不暗示已经接入正式业务。

## 11 定稿时需要确认的少量事项

- 品牌色及整套配色：本稿校园蓝基线仍需结合学校素材核对。
- 视觉数值与素材风格：通过少量组件及内容样张确认字体层次、圆角、图片和图标，统一定稿；无需逐页重谈。
- 目标设备与 APP 框架确定后，核对系统字体、原生控件、导航转场及逻辑尺寸映射。

## 12 参考依据

- [Apple 界面设计基础](https://developer.apple.com/design/tips/)：触摸区域、对比度、图片比例与对齐原则。本文的具体配色、间距与圆角属于项目建议。
- [Android 点击区域](https://support.google.com/accessibility/android/answer/7101858)：48 dp 点击区域及视觉图标与实际点击范围的区别。
- [Material 颜色角色](https://github.com/material-components/material-components-android/blob/master/docs/theming/Color.md)：通过语义颜色及成对前景、背景统一组件外观。
- [WCAG 文字对比度](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)：普通文字 4.5:1 门槛及实际背景上的验证方式。
- [Things](https://culturedcode.com/things/features/)、[Apple 健康](https://support.apple.com/en-lamr/104997)、[Airbnb 官方界面](https://news.airbnb.com/product-releases/airbnb-2025-summer-release)、[Wonderous](https://github.com/gskinnerTeam/flutter-wonderous-app)：用于理解信息层次、内容构图与组件表现，不作为业务功能或技术选型的确认依据。
