# AGENTS.md

本仓库维护 Metaversity 的 UI 设计规范和静态交互展示，使用 HTML、CSS 与原生 JavaScript。根目录规范是设计依据，`index.html` 是对应展示；`assets/fonts/` 保存共用字体样式、官方字体及授权，`docs/development/` 保存每次任务的中文计划、结果和验证记录。

- 开始修改前先写任务开发日志；只修改本次目标涉及的文件。
- 字体统一使用阿里巴巴普惠体 3.0，复用 `assets/fonts/typography.css` 的字号、字重、行高变量；字体文件保持官方原版并保留完整法律声明。
- 修改规范时同步展示页、README 和 Metaversity 应用中的同名规范及字体变量；两仓库的业务示例可以不同，文字规则必须一致。
- 表单字段使用红色 `*` 标记必填，选填不加后缀；条件必填同步标记和校验。展示页只模拟前端反馈，实际应用必须独立执行后端校验。
- 本地运行 `python -m http.server 8000`；检查资源加载、手机与桌面布局、文字放大和示例交互，不引入构建或测试框架。
- 当前为 v0.3：字体和必填标识已统一，其他视觉基线继续按规范维护。推送前检查差异并获取远端最新状态；只提交本次核对的改动，推送 main 后核对远端和 Pages 状态。
