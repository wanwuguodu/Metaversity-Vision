# AGENTS.md

本仓库维护 Metaversity 的 UI 设计规范和静态交互展示，使用 HTML、CSS 与原生 JavaScript。根目录规范是设计依据，`index.html` 是对应展示；`assets/fonts/` 保存共用字体样式、官方字体及授权，`docs/development/` 保存每次任务的中文计划、结果和验证记录。

- 开始修改前先写任务开发日志；只修改本次目标涉及的文件。
- 字体统一使用阿里巴巴普惠体 3.0，复用 `assets/fonts/typography.css` 的字号、字重、行高变量；字体文件保持官方原版并保留完整法律声明，修改字体样式时同步应用与展示页的引用版本。
- 修改规范时同步展示页、README 和 Metaversity 应用中的同名规范及字体变量；实际应用与设计展示可使用不同业务内容，共用交互和文字规则必须一致。
- 表单字段使用红色 `*` 标记必填，选填不加后缀；条件必填同步标记和校验。展示页只模拟前端反馈，实际应用必须独立执行后端校验。
- 所有后续相关列表示例沿用 UI 规范 7.1、7.2：搜索常驻、筛选默认收起，电脑锚定浮层、手机底部浮层，展开不推挤列表；普通控件至少 44px、按内容换行。起止日期或时间在一个框内并排显示，先开始后结束、最终成对确认、取消保留原值。实际应用复用 FilterBar、WheelRangePicker，不另建平行实现；填写表单和主要操作保留在页面上。
- 一级页顶部使用紧凑搜索框，只查功能入口与功能大分类；学校切换位于个人页，业务页不放公共演示环境横幅。上下导航按规范第 8 节维护，保留点击区域、文字放大与系统安全区。
- 手机与电脑共用语义字体变量，手机（不大于 600px）采用标题 20/28、分区 16/24、正文 14/20、辅助 13/20；实际输入均为 16/24。HTML 手机组合示例始终展示手机档位，容器按规范 5.1 收紧并保留至少 44px 点击区。
- 单一操作入口整框点击，图标不另设按钮；下拉触发框与选项浮层按规范 7.3、7.4 保持统一圆角、文字、勾选与键盘状态。
- 本地运行 `python -m http.server 8000`；检查资源加载、手机与桌面布局、文字放大和示例交互，不引入构建或测试框架。
- 当前为 v0.7：字体、必填标识、筛选浮层、起止范围、紧凑导航、全局搜索、手机密度、整框入口及圆角下拉已统一，其他视觉基线继续按规范维护。规范和 HTML 与应用仓库同步，HTML 仅字体资源路径不同。推送前检查差异并获取远端最新状态；只提交本次核对的改动，推送 main 后核对远端和 Pages 状态。
