# 整框入口与圆角下拉选择

## 任务目标

同步应用的整框入口与圆角下拉规范，更新 HTML 中的交互示例。

## 实施计划

1. 更新单一操作入口的整框点击规则和示例。
2. 更新下拉浮层的圆角、字体、选项状态与键盘操作。
3. 与应用统一 UI 规范及展示页保持一致，验证手机和桌面后提交、推送并核对线上发布。

## 完成结果

规范、AGENTS、README 与 HTML 同步为 v0.7。单一操作卡片、列表、形状示例整框可点；5 个下拉改为圆角选项浮层，原表单值、筛选和重置逻辑保持。

## 验证情况

应用与 Vision 规范逐字一致，两个 HTML 仅字体路径不同；内联脚本解析通过。已验证整框点击、手机和桌面下拉、筛选结果、方向键与 Enter、清除条件，以及 320px 下 125% 文字放大；最终页面没有运行错误或横向溢出。最终差异检查通过，按 main 分支提交与同步发布。
