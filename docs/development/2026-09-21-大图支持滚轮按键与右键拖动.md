# 大图支持滚轮按键与右键拖动

## 任务目标

同步校园应用的大图手势：电脑按住鼠标滚轮（中键）或右键均可拖动，滚轮旋转继续缩放。

## 实施计划

同步内联手势函数、操作提示、规范、README 和项目规则；核对与应用展示的一致性，提交推送并检查 Pages 发布。

## 完成结果

- 展示页内联手势同时支持中键和右键拖动，阻止画布内中键默认动作；同步提示、规范、README 和项目规则。

## 验证情况

- 与校园应用的规范文件完全一致，HTML 除字体路径外完全一致；最终差异检查通过。
- 提交 `19817a50a002d95b132edea82d5f23f786d5ee06` 已推送；Pages 工作流 35582136022 成功，Pages 构建接口确认该提交状态为 built。
- Browser 页面操作工具连续超时，本次未完成真实浏览器拖动验收；已完成内联手势、注册／解绑及两份展示的一致性核对。
