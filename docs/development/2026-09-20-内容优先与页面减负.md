# 内容优先与页面减负

## 任务目标
- 同步用户确认的首页、预约入口和页面说明清理规则。

## 实施计划
- 同步应用 UI 规范，补充轻量入口与预约列表样例，更新规则和 README。
- 检查双仓规范与 HTML 一致性，提交并推送。

## 完成结果
- 同步应用 v0.12 规范，明确首页保留高频内容、完整目录单独进入、预约改用轻量分隔列表。
- 增加首页与预约样例，目录支持进入和返回并恢复焦点，复用现有图标与校园图片。设计讲解留在样例外。
- 更新 AGENTS 与 README，后续业务样例不再展示演示、测试、实现或内部元数据说明，保留必要业务信息。

## 验证情况
- 与应用规范逐字一致，HTML 除字体路径外一致；两个可执行内联脚本语法、嵌入 JSON 和新样例唯一 ID 检查通过。
- `git diff --check` 通过，提交前获取远程最新状态，`main` 无新增差异。
- 浏览器自动安全策略拒绝打开本地 HTML 文件地址，未绕过限制；此次未完成手机/桌面实际渲染与交互验收，不将静态检查当作视觉验收。
- 提交后核对远端提交及 GitHub Pages 构建状态，结果随任务反馈。
