# 统一图片与地图缩放拖动手势

## 任务目标

同步手机双指缩放、单指拖动，电脑滚轮缩放、右键拖动的统一规则及交互示例。

## 实施计划

更新 UI 规范、AGENTS、README 与 HTML 大图展示，验证缩放中心、拖动、重置及页面滚动，再提交并推送。

## 完成结果

已升级 v0.8，同步 UI 规范 8.1、规则和 README。图集及原图弹窗接入与应用相同的手势函数；保留前后切换、比例和适配图片操作，不再拖动翻页。

## 验证情况

- 两份规范一致，HTML 仅字体路径不同；公共函数 27 项针对性检查通过，可执行内联脚本通过语法检查。
- 浏览器验证前后翻页及缩放复位、非当前图禁止聚焦、原图弹窗；检查 1280px、390px、320px 和文字放大，页面与工具区没有横向溢出。
- 双指缩放和右键移动由事件检查验证，未宣称真机触摸实测；保留键盘和按钮入口。
- 功能提交 d021807 已推送 main，Pages 发布成功；线上 HTML 与本地文件逐字节一致。
